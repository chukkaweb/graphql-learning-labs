{
  "name": "bytes-rest-router",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "./sh_stitch.sh && rover dev --supergraph-config supergraph.yaml --router-config router.yaml",
    "compose": "rover dev --supergraph-config ./supergraph.yaml",
    "pub": "rover subgraph publish blog-ql-tffss@current --schema 'meta.graphql' --name healthcheck --routing-url 'http://localhost:4000'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.15.0",
  "devDependencies": {
    "cross-env": "^10.0.0",
    "dotenv": "^17.2.2"
  }
}
